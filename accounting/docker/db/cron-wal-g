# Set cron job to create full backup every day at 2 AM
0 2 * * * /usr/local/bin/wal-g backup-push >> /var/log/cron.log 2>&1
# Set cron job to delete old backups every day at 3 AM
0 3 * * * /usr/local/bin/wal-g delete --confirm retain 7 >> /var/log/cron.log 2>&1
